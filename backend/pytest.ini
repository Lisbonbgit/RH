[pytest]
# backend/ no sys.path -> permite "import faturacao..." nos testes
# .. (a raiz do repositorio) -> permite "import agente_impressao...", o programa
# de impressao da loja, que vive fora do backend porque nao e' servidor nenhum
pythonpath = . ..
testpaths = tests
python_files = test_*.py
