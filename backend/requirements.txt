# ===== RH grupo Lisbonb - Backend (FastAPI) =====
# Dependências limpas: apenas o que o server.py realmente usa.
# (Removidas as dezenas de libs do Emergent: google, openai, stripe, boto3, etc.)

# --- Servidor web / ASGI ---
fastapi==0.110.1
uvicorn[standard]==0.25.0
starlette==0.37.2
python-multipart==0.0.21

# --- Base de dados (MongoDB / Atlas) ---
motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0          # necessário para ligações mongodb+srv:// (Atlas)

# --- Validação / modelos ---
pydantic==2.12.5
email-validator==2.3.0    # para o tipo EmailStr

# --- Autenticação / segurança ---
PyJWT==2.10.1
bcrypt==4.1.3

# --- Configuração ---
python-dotenv==1.2.1

# --- Emails (recuperação de palavra-passe) ---
resend>=2.0.0

# --- Integrações externas (ex.: Google Places para avaliações) ---
httpx==0.28.1

# --- Fusos horários (zoneinfo "Europe/Lisbon" em qualquer SO) ---
tzdata==2025.2
