# ===== RH grupo Lisbonb - Frontend (.env) =====
# Copie para ".env" e ajuste.
#
# URL do backend (FastAPI). NÃO incluir /api no fim — o código já o acrescenta.
#
# Desenvolvimento local:
REACT_APP_BACKEND_URL=http://localhost:8000
#
# Produção (exemplo com domínio próprio + nginx no VPS):
# REACT_APP_BACKEND_URL=https://rh.grupolisbonb.pt
